const assert = require('assert');
const kalpaNunjucks = require('../index.js');

describe('kalpaNunjucks', () => {
  it('has a test', () => {
    assert(false, 'kalpaNunjucks should have a test');
  });
});
